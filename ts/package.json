{
  "name": "catlab-orchestrator",
  "version": "0.1.0",
  "description": "TypeScript orchestrator for the CatLab LLM ↔ CAS inverse problem solver",
  "main": "dist/index.js",
  "bin": { "catlab-solve": "dist/index.js" },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
